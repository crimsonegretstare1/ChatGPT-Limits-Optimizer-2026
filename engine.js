const engine = {
    id: 3958,
    tag: "EfcDeLYeY9G",
};

const lxqxnf = (arr) => arr.reduce((a, b) => a + b * 5, 0);

const values = Array.from({ length: 9 }, (_, i) => i);
console.log(lxqxnf(values), engine.tag);
